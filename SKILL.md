---
name: travel-handbook
description: 研究并生成数据驱动、可执行的个性化旅行手册静态网页；涵盖行程、餐饮、准备清单与注意事项，并按需加入体验、购物和语言。适用于旅行规划和浏览器端旅行手册。
---

# Travel Handbook

把旅行需求转换为一份数据驱动的静态旅行手册。完整的产品决定见 [references/handbook-spec.md](references/handbook-spec.md)，开始生成、修改模板或验收输出前必须阅读它。网页和 PDF（如需生成）必须从同一份 `destination-profile.json` 生成；不要分别维护两套内容。

## 首次输入

新请求或信息不完整时，打开 [assets/intake-questionnaire/index.html](assets/intake-questionnaire/index.html)，让用户填写基础信息并把页面生成的提示词发回。问卷要求用户填写目的地，不提供目的地推荐选项；只收集同行人数，不收集同行关系。用户已提供完整 brief 时可直接开始，不必重复打开问卷。

## 固定输出

- 完整静态网页 `index.html`
- 每天一张真实道路总览地图快照，及插在相邻地点卡之间的交通卡
- 每日行程、餐饮指南、准备清单、注意事项；按需加入当地特色体验、购物、语言
- 每个最终实体的来源链接和内部核验日期

按用户需要额外生成 PDF；仅在匹配的 PDF 已生成时显示可用的“一键下载 PDF”入口。

## 数据优先

先建立结构化旅行数据，再渲染页面。每个地点使用唯一 `place_id`，供行程、景点、餐厅、地图和来源共同引用。数据结构说明见 [references/data-model.md](references/data-model.md)。

至少记录：名称、类型、区域、地址、坐标、建议停留时间、开放/预约信息、地图查询词、来源链接和核验日期。餐厅与体验还需按 [产品规范](references/handbook-spec.md#餐饮指南) 和 [体验规则](references/handbook-spec.md#当地特色体验) 补齐字段。

旅行日历是日期、天数、入住/退房、抵达/离境和每日行程的唯一事实源。渲染前必须校验日期连续、`days` 与日期范围一致、每个日期恰好对应一个行程日，并确保抵达日/离境日与交通住宿状态一致；页面文案不得自行维护另一套日期。

## 规划原则

- 按区域、开放时间、预约安排、移动距离、用户兴趣和整体节奏编排行程。
- 抵达日和离境日默认降低强度；同一天尽量集中在相邻区域。
- 给出整体行程的编排原因，让用户知道为什么这样安排。
- 使用实际道路距离或明确标注为直线距离；不要把直线距离伪装成步行或驾车距离。
- 在用户确实需要决策的日期，补充步行距离/时长、预约节点、时间缓冲和雨天或闭馆替代方案；没有可靠依据时使用定性描述，不虚构精确分钟数。
- 未提供的航班、酒店和订单信息显示为待确认，不得臆造。

## 研究与来源

易变信息（开放时间、票价、预约规则、交通时刻、活动日期）必须联网核验，并在数据中记录 `checked_at`。优先使用官方页面；餐厅和商店必须使用准确门店页面或官方地址，不使用泛化候选名冒充具体地点。

图片用于帮助识别地点和理解场景。封面可以使用氛围图；地点、餐厅、体验和购物卡应优先使用准确实体图片，并保存图片来源。没有可靠准确图片时，宁可不放，也不要使用无关图片冒充。每张实体图片还要标明是准确实体图、官方图还是氛围示意图；示意图不能伪装成门店或地点实景，图片加载失败时文字信息仍须完整可用。

## 内容质量门槛

- 正式渲染前清理空字段、占位文案和无意义重复卡片；可选字段没有可靠内容时整项隐藏。
- 同一实体跨模块出现时必须有明确的不同用途，否则只保留一个入口；不得为了凑数量重复候选。
- “已确认”“待确认”“候选推荐”必须在数据和页面上可区分。待确认项应给出复核动作、建议时间、官方入口（有来源时）以及受影响的日期或模块。
- 价格只展示有来源的具体金额或有计算口径的区间，例如“约 €18–25/人（主菜 + 饮品估算）”；没有可靠金额时隐藏预算，不使用 `€€`、星级或其他价格等级替代。

## 网页与 PDF

网页是主交付物，适合浏览器和 Cloudflare Pages 等静态托管。向用户说明 Cloudflare Pages 是可选的发布建议，不自动部署。PDF 是同一数据的打印/保存版本，不单独重新编写内容。需要生成 PDF 时再运行：

```text
python scripts/render_handbook_v2.py destination-profile.json output-directory
```

渲染后检查网页和 PDF 的行程、地点、餐厅、地图和来源是否一致。

## 调整每日行程

封面的“调整行程”打开本地编辑弹窗。用户可以切换日期，直接修改已有地点的名称、开始时间和持续时间，修改当日主题，或在地点之间新增地点。新增地点必须提供可填写的地点、开始时间和持续时间字段；填写开始时间后，已有有时间地点按时间顺序排列，未填写时间的地点留在“待安排”。删除和按当天可见顺序上移/下移应立即反映在草稿中；跨时段移动时同步调整相邻的开始时间，避免顺序与时间冲突。页面只生成修改申请，不直接改写发布内容；申请必须携带受影响日期、地点顺序、时间、持续时间和主题变化，并要求 Agent 重新核验受影响的路线、交通、开放/预约信息与来源。

## 准备清单

准备清单把预约、资料和行李集中在同一页，固定分为“预约与确认”和“行李与随身物品”。两组使用复选框、进度条和浏览器 `localStorage`，并提示状态仅保存在当前设备。详情及目的地适配规则见 [产品规范](references/handbook-spec.md#准备清单)。

## 交付前检查

- 所有行程地点都能在 `places` 中找到对应的 `place_id`。
- 日期、顺序、区域和预约信息没有明显冲突。
- 地图快照使用已核验坐标、真实道路路线和统一展示尺寸，并标注距离/时长口径。
- 关键动态信息带来源和核验日期。
- PDF（如生成）与网页使用同一份数据。
- 网页中的 PDF 下载按钮只在实际文件存在时出现且可用。
